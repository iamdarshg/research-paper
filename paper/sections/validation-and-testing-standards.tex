\section{Validation and Testing Standards}
\label{sec:validation}
To make the validation story honest, we separate three levels of evidence:
\begin{itemize}
    \item \textbf{Code-path validation:} the training, generation, cleanup, export, and CFD scoring routines run end-to-end in this environment.
    \item \textbf{Solver validation:} the built-in CFD path can be compared against a higher-fidelity external solver when one is available.
    \item \textbf{Physical validation:} the generated shape should eventually be compared against analytic or experimental benchmarks, which is still future work.
\end{itemize}

\subsection{Verification}
Verification checks that the implemented steps are executed correctly. In this revision we verified that:
\begin{itemize}
    \item the shape-plausibility loss contributes to training,
    \item the connected-component cleanup hook reduces fragmented voxel outputs,
    \item STL export succeeds from the cleaned voxel grid, and
    \item the OpenFOAM export hook writes a runnable case directory skeleton, including `blockMeshDict`, `snappyHexMeshDict`, and surface STL output.
\end{itemize}

\subsection{Validation}
Validation is now reproducible on a single local machine with OpenFOAM installed. The built-in CFD path is exercised on the reduced CPU pipeline, and the OpenFOAM sonicFoam benchmark is run on the shared centered-cube validation object. The comparison uses the lower-level `forces` output when available, with a manual pressure integration fallback only if the function-object output is missing.

The paper therefore claims the following and nothing more: the export path is concrete, the benchmark comparison is measurable, and the shared validation geometry is documented explicitly so the solver-to-solver check can be repeated without ambiguity.

\subsection{Experimental Design and Baseline Selection}
To rigorously evaluate the proposed framework, we define a set of baseline comparisons and ablation studies. These baselines are selected to provide context for the generative performance and aerodynamic efficiency of our method.

\subsubsection{Baseline Families}
We compare our method against three primary families of baselines:
\begin{enumerate}
    \item \textbf{Classical Design:} Parametric aircraft primitives and hand-designed bodies based on standard NACA airfoils.
    \item \textbf{Generative 3D:} Traditional voxel-GAN architectures and point-cloud diffusion models without aerodynamic guidance.
    \item \textbf{Optimization-based:} SIMP-based topology optimization and surrogate-based aerodynamic shape optimization (ASO).
\end{enumerate}

\subsubsection{Ablation Studies}
We investigate the contribution of individual components through the following ablations:
\begin{itemize}
    \item \textbf{No CFD Loss:} Evaluating the impact of aerodynamic guidance on shape efficiency.
    \item \textbf{No Connectivity Prior:} Assessing structural viability without the connectivity loss term.
    \item \textbf{Resolution Sweep:} Testing the scalability of the progressive training schedule across 16\textsuperscript{3}, 32\textsuperscript{3}, and 64\textsuperscript{3} resolutions.
\end{itemize}

\subsubsection{Claim-to-Baseline Mapping}
Table \ref{tab:experimental_plan} summarizes our primary research claims and the corresponding experimental validation strategy.

\begin{table}[h]
\centering
\caption{Experimental Plan and Claim Mapping}
\label{tab:experimental_plan}
\footnotesize
\begin{tabular}{|p{2.5cm}|p{2.0cm}|p{1.5cm}|p{1.5cm}|}
\hline
\textbf{Paper Claim} & \textbf{Baseline / Ablation} & \textbf{Metric} & \textbf{Validation Run} \\ \hline
Aerodynamic efficiency improvement & Classical design, No CFD loss & $L/D$ Ratio & High-res D3Q27 \\ \hline
Structural connectivity & No connectivity prior & Label count & 32\textsuperscript{3} Generation \\ \hline
Fast multi-fidelity design & ASO baseline, GAN baseline & Runtime vs. Accuracy & Multi-step sweep \\ \hline
Generalization to mission profiles & Parametric primitives & Design diversity & Conditioned batch gen \\ \hline
\end{tabular}
\end{table}
